# Operations and Deployment

## Local infrastructure

The repository uses Docker Compose for local PostgreSQL, Redis, and MinIO services. The configuration is defined in [docker-compose.yml](../../docker-compose.yml).

## Current deployment posture

The codebase is ready for local development and MVP validation, but there is no production deployment workflow documented in the source tree beyond the local Docker-based setup.
